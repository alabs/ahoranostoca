<div class="border-meetings-top"></div>
<section id="meetings" class="wrapper-home upcoming-events bg-yellow section-over section-padding-8">
  <div class="row">
    <div class="columns small-centered large-10 text-center mb-3">
      <h2 class="text-uppercase" style="font-size:2.2rem;line-height:1.1">Próximas actividades</h2>
    </div>
    <div class="row">
      <div class="columns medium-6">
        <% upcoming_events.first(4).each do |event| %>
          <%= card_for event, size: :s %>
        <% end %>
      </div>
      <div class="columns medium-6">
        <% if geolocation_enabled? %>
          <%= cell "decidim/meetings/meetings_map", upcoming_events %>
        <% else %>
          <% (upcoming_events - upcoming_events.first(4)).each do |event| %>
            <%= card_for event, size: :s %>
          <% end %>
        <% end %>
      </div>
    </div>
    <div class="row">
      <div class="section-center columns small-centered small-12
        smallmedium-8 medium-6 large-4">
        <%= link_to t("decidim.meetings.content_blocks.upcoming_events.view_all_events"), meetings_directory_path, class: "button button-black button--sc pull-right" %>
      </div>
    </div>
  </div>
</section>
<div class="border-meetings-bottom"></div>
